{% extends 'base.html.twig' %}

{% block title %}AccesInfoService - Backoffice - Les sous services{% endblock %}

{% block content %}
    <div class="container">
    <h1 class="pt-5 pb-5">Gestion du sous-service {{ detail_service.titre }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail_service.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ detail_service.titre }}</td>
            </tr>
            <tr>
                <th>Subtitle</th>
                <td>{{ detail_service.subtitle }}</td>
            </tr>
            <tr>
                <th>Lié au service</th>
                <td>{{ detail_service.service.titre}}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ detail_service.contenu }}</td>
            </tr>
            <tr>
                <th>Url Image</th>
                <td>{{ detail_service.urlImg }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>
                    <img class="logoServiceBackoffice" src="/images/Sous-service/{{ detail_service.UrlImg }}">
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('detail_service_index') }}">Liste</a>

    <a href="{{ path('detail_service_edit', {'id': detail_service.id}) }}" class="ml-3">Editer</a>
    
    {{ include('detail_service/_delete_form.html.twig') }}
    </div>
{% endblock %}
